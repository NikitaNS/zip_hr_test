import {StyleSheet} from 'react-native';
import {colors} from '../../../constans';

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: '600',
    color: colors.white,
  },
});
